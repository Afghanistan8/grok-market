# The app is locked to GenLayer studionet (chain 61999). The network and the
# contract address are fixed in src/lib/env.ts, so nothing here is required.

# Optional. Injected wallets (MetaMask etc.) work without it.
VITE_WALLETCONNECT_PROJECT_ID=
